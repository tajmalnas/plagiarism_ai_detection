def detect_ai_content(essay):
    # Placeholder: Add logic to detect AI-generated text using tools like GPTZero
    return "AI-generated content detected!" if "GPT" in essay else "Human-written content."
